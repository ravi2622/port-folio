# Local development
MONGO_URI=mongodb://127.0.0.1:27017/portfolio
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Render production — paste in Render dashboard ONLY (not in git)
# Replace YOUR_PASSWORD with Atlas password (URL-encode special chars: @ → %40)
# MONGO_URI=mongodb+srv://ravi2622:YOUR_PASSWORD@cluster0.brkpl.mongodb.net/portfolio?retryWrites=true&w=majority&appName=Cluster0
